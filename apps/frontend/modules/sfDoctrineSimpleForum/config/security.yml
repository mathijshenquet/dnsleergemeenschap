createTopic:
  is_secure:   true

addTopic:
  is_secure:   true

addPost:
  is_secure:   true
  
deletePost:
  is_secure:   true
  credentials: [moderator]

deleteTopic:
  is_secure:   true
  credentials: [moderator]
    
toggleStick:
  is_secure:   true
  credentials: [moderator]

toggleLock:
  is_secure:   true
  credentials: [moderator]